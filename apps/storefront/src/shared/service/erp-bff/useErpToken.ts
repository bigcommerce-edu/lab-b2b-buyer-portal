import { useAppSelector } from "@/store";

export default function useErpToken() {
  // TODO: Get the ERP token from the Redux store and return it
  //  - Use `useAppSelector` with the `selectErpToken` selector imported from the `erp` slice
  throw new Error('useErpToken not implemented');
}
