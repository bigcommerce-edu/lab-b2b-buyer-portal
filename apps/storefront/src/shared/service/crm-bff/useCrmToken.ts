import { useAppSelector, selectCrmToken } from "@/store";

export default function useCrmToken() {
  // TODO: Get the CRM token from the Redux store and return it
  //  - Use `useAppSelector` with the `selectCrmToken` selector imported from the `crm` slice
  throw new Error('useCrmToken not implemented');
}
