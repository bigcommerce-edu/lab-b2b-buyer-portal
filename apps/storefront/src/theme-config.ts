import { Theme, ThemeOptions } from "@mui/material"

export const getThemeConfig = (theme: Theme) => {
  // TODO: Return a theme options object
  //  - Apply a custom font with `typography.fontFamily`
  throw new Error('getThemeConfig not implemented');
}
